@inject ITranscriptBodyRenderer Renderer

<div class="@CssClass">@((MarkupString)Renderer.Render(Body))</div>

@code {
    [Parameter] public string Body { get; set; } = "";
    [Parameter] public string? Class { get; set; }

    string CssClass => string.IsNullOrWhiteSpace(Class)
        ? "app-markdown"
        : $"app-markdown {Class}";
}
