<AppButton Variant="@Variant" Size="icon" Active="@Active" Disabled="@Disabled" Title="@Title" AriaLabel="@Title" Class="@Class" OnClick="@OnClick">
    <FaIcon Name="@Icon" />
    <span class="sr-only">@Title</span>
</AppButton>

@code {
    [Parameter] public string Icon { get; set; } = "circle";
    [Parameter] public string Variant { get; set; } = "ghost";
    [Parameter] public bool Active { get; set; }
    [Parameter] public bool Disabled { get; set; }
    [Parameter] public string Title { get; set; } = "";
    [Parameter] public string? Class { get; set; }
    [Parameter] public EventCallback<MouseEventArgs> OnClick { get; set; }
}
